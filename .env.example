# Frontend environment for AkuAI views
# If your API is same-origin (served under the same domain), leave this empty and the app will use relative paths.
# If your API is on a different origin, set the base here, e.g.:
# VITE_API_BASE=https://api.yourdomain.com
VITE_API_BASE=http://127.0.0.1:5000
